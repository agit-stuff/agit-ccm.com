---
title: Configuration
description: Configure AGIT for your workflow
---

AGIT works out of the box with sensible defaults, but you can customize its behavior.

## Generated Files

When you run `agit init`, these files are created:

### `.agit/` Directory

The neural graph storage, similar to `.git/`:

```
.agit/
├── objects/     # Content-addressable object store
├── refs/        # Branch references
│   └── heads/   # Branch head pointers
├── HEAD         # Current branch pointer
└── index        # Staging area (JSONL format)
```

### Editor Configuration Files

AGIT generates configuration files for AI editors:

| File | Editor | Purpose |
|------|--------|---------|
| `CLAUDE.md` | Claude Code | Instructions for Claude to log thoughts |
| `.cursorrules` | Cursor | Instructions for Cursor AI |
| `.windsurfrules` | Windsurf | Instructions for Windsurf AI |

These files tell your AI assistant to:
1. Call `agit_log_step` when planning
2. Record user intents and AI reasoning
3. Help create meaningful commit summaries

## Customizing Editor Rules

You can edit the generated files to customize how your AI assistant logs thoughts.

### Example: Custom Categories

By default, AGIT uses `intent` and `reasoning` categories. You can add custom categories:

```markdown
<!-- In CLAUDE.md -->
When logging thoughts, use these categories:
- intent: What the user wants to achieve
- reasoning: Your plan/approach
- decision: Key technical decisions made
- alternative: Alternatives considered but rejected
```

## Environment Variables

AGIT respects these environment variables:

| Variable | Description | Default |
|----------|-------------|---------|
| `AGIT_DIR` | Custom AGIT directory name | `.agit` |
| `RUST_LOG` | Log level for debugging | `warn` |

### Example: Debug Mode

```bash
RUST_LOG=debug agit commit -m "Debug commit"
```

## Git Integration

AGIT works alongside Git, not replacing it:

1. **Parallel Storage**: `.agit/` lives alongside `.git/`
2. **Linked Commits**: Neural commits reference git commits
3. **Branch Tracking**: AGIT follows your git branches

### Recommended Git Workflow

```bash
# 1. Work on your feature with AI assistance
#    (AI automatically logs thoughts)

# 2. Stage and commit code
git add .
git commit -m "Add feature X"

# 3. Create neural commit
agit commit -m "Add feature X"
```

## MCP Server Configuration

The AGIT MCP server starts automatically when needed. For manual configuration:

```bash
# Start server manually
agit server

# With custom port (if needed)
agit server --port 8080
```

## Ignoring AGIT Files

Add to your `.gitignore` if you don't want to share neural commits:

```gitignore
# Ignore AGIT data (optional)
.agit/
```

However, we recommend **committing** the editor config files so your team shares the same AI instructions:

```gitignore
# Don't ignore these
# CLAUDE.md
# .cursorrules
# .windsurfrules
```

## Next Steps

- [Set up Cursor integration](/guides/cursor-setup/)
- [Set up Claude Code integration](/guides/claude-code-setup/)
- [Learn about MCP integration](/guides/mcp-integration/)
